import test from "ava";
// If you needs to import your files in src, you needs to require lib-es5 folder.
// import A from "../lib-es5/A";

test.todo("We recommend you to write test for reducing bug by your updates.");

test('This is just a sample test',(t)=>{
  t.truthy(10 * 5 === 50);
});
