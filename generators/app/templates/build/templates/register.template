const _$ns = GrimoireInterface.ns("{{namespace}}");
{{#each components}}
  GrimoireInterface.registerComponent(_$ns("{{key}}"),{{key}});
{{/each}}

{{#each converters}}
  GrimoireInterface.registerConverter(_$ns("{{key}}"),{{key}});
{{/each}}
