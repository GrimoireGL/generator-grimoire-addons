{
  "name": "<%= name %>",
  "version": "0.0.0",
  "description": "<%= desc %>",
  "main": "src/index.js",
  "keywords": [
    "grimoire.js"
  ],
  "dependencies": {},
  "devDependencies": {
  },
  "repository": "<%= repo %>",
  "scripts": {
    "build": "babel-node rollup.config.js --presets es2015,stage-2 --plugins transform-runtime",
    "prepublish": "babel-node ./build/postinstall.js --presets es2015,stage-2 --plugins transform-runtime"
  },
  "grimoire":{
    "namespace":"HTTP://GRIMOIRE.GL/NS/CUSTOM",
    "dependencies":[]
  },
  "license": "MIT"
}
